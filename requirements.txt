# Core runtime dependencies
PyYAML>=6.0
numpy>=1.21
opencv-contrib-python>=4.10.0

# MMPose stack (versions pinned to the 1.x / mmengine-based branch)
mmengine>=0.8.0
mmcv>=2.0.0
mmdet>=3.0.0
mmpose>=1.2.0

# Development / testing
pytest>=8.0.0

# NOTE: Install torch/torchvision separately to match your CUDA/CPU environment (see docs/環境構築手順.md).
